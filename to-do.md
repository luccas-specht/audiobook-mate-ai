### Save the PDF where?
    -> Should store in a folder called temp. This folder should move to production as well. 

### Save the moment the user stops listening and then go back to that part of the book? 

 -> Somehow save that info (the local the user paused) in a cookie?
 -> What'd defined it? a group of details? -> (the page -> paragraph -> the line -> the column)


### What AI provides such behavior? 
  -> Identifies the who is the character reader? Male or Female?
  -> Understand the text and reads it in a such human reader voice
  -> The AI should skip the summary
  -> The project should start reading the book title -> the author name -> the Dedication. After those ones. Start the Prologue if exists. else start reading the book itself


### What read?
- the summary is still being send to gemini voice. It goes reading/speaking chapter per chapter name... The user want it? Or we can just skip it?